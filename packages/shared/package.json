{
  "name": "@monad-session-arena/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared types, constants and ABIs for Monad Session Arena.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo \"No shared tests yet\"",
    "clean": "rm -rf dist"
  },
  "devDependencies": {}
}
